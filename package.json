{
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/decoder",
    "packages/loader",
    "packages/node",
    "packages/tokenizer"
  ],
  "scripts": {
    "precompile": "turbo compile",
    "compile": "tsc --build",
    "compile:clean": "tsc --build --clean",
    "compile:watch": "tsc --build --watch --preserveWatchOutput",
    "preformat": "turbo format",
    "format": "prettier --write .",
    "preformat:check": "turbo format:check",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "pretest": "turbo test",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --silent",
    "start": "node packages/node/lib/main.js"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@swc/core": "^1.3.96",
    "@swc/jest": "^0.2.29",
    "@types/node": "^20.9.0",
    "@typescript-eslint/eslint-plugin": "^6.10.0",
    "@typescript-eslint/parser": "^6.10.0",
    "eslint": "^8.53.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-import": "^2.29.0",
    "eslint-plugin-markdown": "^3.0.1",
    "jest": "^29.7.0",
    "prettier": "^3.0.3",
    "turbo": "^1.10.16",
    "typescript": "^5.2.2"
  },
  "wallaby": {
    "env": {
      "params": {
        "runner": "--experimental-vm-modules"
      }
    }
  }
}
